{{page-title "Customer"}}

<h1>Customer Details</h1>

<ul>
    <li>id: {{@model.id}}</li>
    <li>name: {{@model.name}}</li>
    <li>age: {{@model.age}}</li>
    <li>email: {{@model.email}}</li>
</ul>

<LinkTo @route="customers">
    Go Back
</LinkTo>